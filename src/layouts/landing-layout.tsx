import type { FC } from 'hono/jsx'

export type LandingLayoutProps = {
}
export const LandingLayout: FC<LandingLayoutProps> = (props: LandingLayoutProps) => (
  <div> 
    <h1> Landing Page </h1>
  </div>
)
export default LandingLayout
